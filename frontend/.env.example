# Copia a .env. Mismo Client ID que GOOGLE_CLIENT_ID en backend/.env (un solo
# proyecto OAuth de Google Cloud, dos lugares que lo necesitan). Sin esto, el
# botón de login no aparece y nadie puede evaluarse: iniciar sesión es
# obligatorio para el chat y los tests.
VITE_GOOGLE_CLIENT_ID=
